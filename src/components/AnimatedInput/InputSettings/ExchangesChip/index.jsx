import React, {useState, useEffect} from 'react';
import './styles.css';

const ExchangesChip = ({ defaultValue = 3 }) => {
  const [exchanges, setExchanges] = useState(defaultValue);

  return (null);
};

export default ExchangesChip;
